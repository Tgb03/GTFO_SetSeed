<Project>
    <PropertyGroup>
        <TSName>SetSeed</TSName>
        <TSAuthor>tgb03</TSAuthor>
        <TSVersion>1.0.1</TSVersion> <!-- TSVersion empty = GitInfo used instead (if available; check nuget) -->
        <TSWebsite>https://github.com/Tgb03/GTFO_SetSeed</TSWebsite>
        <TSDescription>Allows you to set the SessionSeed in game using chat commands.</TSDescription>
    </PropertyGroup>
    <ItemGroup>
        <!-- Add the Dependency strings here, one for each dependency -->
        <TSDependencyStrings Include="BepInEx-BepInExPack_GTFO-3.2.1" InProject="false" />
        <!-- <TSDependencyStrings Include="Some-Other-Dependency-1.2.3" InProject="false" /> -->
    </ItemGroup>
</Project>